---
scope: Contact Management MVP - full system architecture (single feature-set MVP, not multi-epic platform)
purpose: Build substrate for BMAD epics/stories + dev implementation
altitude: initiative-to-features (MVP-level, single initiative)
updated: 2026-09-16T11:25
---

- (decision) Working mode: Coaching Path (user explicitly wants alternatives/tradeoffs shown before choosing, not silent drafting) - matches user's standing preference for pros/cons explanations on every technical decision.
- (direction) In addition to the technical ARCHITECTURE-SPINE.md (for Amelia/devs), user wants a second non-technical explainer artifact for management/non-technical stakeholders, to be produced at the Renderings & Polish finalize step.
- (decision) AD: Single Next.js application for MVP (not split into separate admin/public apps). Internal vs public separation enforced at route + auth-middleware level, not at the app/deployment level. Chosen as a 'modular monolith' - packages structured (db/ui/types) so a future split to multiple apps stays cheap if phases 2-4 warrant it.
- (decision) AD: Next.js App Router (not Pages Router). Rationale discussed at length with user: official current best-practice default, Server Actions reduce boilerplate for the project's many forms, RSC data-fetching fits the contacts/bodies list screens well. Server/Client component split is the main learning-curve cost, accepted knowingly.
- (decision) AD: Auth.js (NextAuth) as the auth abstraction layer from day one, starting with a Credentials (email+password) provider for MVP. Specific identity provider (org SSO vs staying on Credentials) explicitly deferred - user doesn't yet know if org has Microsoft/Google/AD SSO. Revisit condition: before Phase 4 (personal area for external body reps) or whenever org IT confirms SSO availability, add provider config only - no changes needed elsewhere since Auth.js abstracts session/route-protection from the specific provider.
- (decision) Local dev database: Postgres via Docker image (user is already doing this) - confirms the earlier bmad-workflow.md recommendation.
- (question) Production hosting target explicitly undecided (Vercel vs self-hosted Docker vs other) - deferred by user's own choice. Revisit condition: before first production deploy. Until decided, the spine must avoid provider-lock-in choices (e.g. no Vercel-proprietary storage APIs) so the app stays portable to either target.
- (decision) AMENDS earlier single-app decision: leadership input clarifies there will ultimately be TWO separate applications - apps/internal (coordinators/super-admin) and apps/public (anonymous registration form). Current MVP implementation work focuses ONLY on apps/internal; apps/public is deferred (scaffolded later, not built now). Monorepo structure must still be set up to accommodate both from the start (packages/db, packages/ui, packages/types shared) even though apps/public isn't built yet, so the later split isn't a rewrite.
- (decision) AD (decision 1 of 3, confirmed): Domain-scoped permission enforcement via centralized middleware (proxy.ts in Next.js 16), not per-endpoint manual checks. Binds: every route handler must go through the shared auth/domain-scope check. Prevents: a forgotten per-endpoint permission check silently exposing cross-domain data.
- (question) File storage mechanism for public registration form uploads (local disk vs S3-compatible object storage) explicitly deferred by user for now. Linked to the already-deferred production hosting decision - revisit together.
- (decision) AD (decision 3 of 3, confirmed): Audit trail via Prisma Client Extensions (), writing through Prisma.getExtensionContext(this) so each audit row shares its triggering mutation's transaction automatically. Multi-model operations (e.g. registration approval) still require an explicit prisma.$transaction(async (tx) => ...) at the call site to group writes atomically. This replaces the initially-drafted non-atomic snippet found during skill review - getExtensionContext resolves correctly to the tx client, avoiding the need for a separate mutateWithAudit wrapper.
- (event) Non-technical explainer artifact published for management/non-technical stakeholders: https://claude.ai/artifact/5sVaEP9RwvMKiXBrou3ctV. Reviewer Gate dispatched: rubric walker + 2 configured finalize_reviewers (version-currency, adversarial two-units-diverge) + input-reconciliation against PRD/UX/tech-stack-decisions.md.
